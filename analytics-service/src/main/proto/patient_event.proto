syntax = "proto3";

package patients.events;
option java_multiple_files = true;

message PatientEvent {
  string patientId=1;
  string name=2;
  string email=3;
  string event_type=4;
}
//proto files would be in central repo and each service will pull it and generate its proto file